{
    "name": "susaekns-website",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "next dev",
        "build": "next build",
        "start": "next start",
        "deploy": "rm -rf node_modules/.cache && next build && next export && touch out/.nojekyll && printf '%s' 'susaekns.com' > out/CNAME && git add out/ && git commit -m 'Deploy Next.js to gh-pages' && git subtree push --prefix out origin gh-pages"
    },
    "dependencies": {
        "bulma": "^0.9.1",
        "keen-slider": "^5.5.1",
        "next": "13.4.9",
        "prop-types": "^15.8.1",
        "react": "18.2.0",
        "react-bulma-components": "^4.1.0",
        "react-dom": "18.2.0",
        "sass": "^1.32.5"
    }
}
